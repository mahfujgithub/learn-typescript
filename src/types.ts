// Primitive Types
// String Types
// Boolean Types
// Number Types
// Null Types
// Undefined

// We can define our variables type explicitly so that, if need redeclare or reassign any variable so that typescript alert you about type of containing data type!

let person: string = "Mahfujur Rahman";
let age: number = 56;
let isHuman: boolean = true;
let mahfuj: null = null;

// If we Don't Define the variable type it will be implicitly defined any type typescript it self;
let student = undefined;
